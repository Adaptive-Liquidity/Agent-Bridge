interface:
  display_name: "Agent Bridge"
  short_description: "Repo-specific patterns and workflows for Agent-Bridge"
  default_prompt: "Use the Agent-Bridge repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true